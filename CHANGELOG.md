# Changelog

## 0.0.3
- Add `Import` module with `fromPngBuffer` and `fromPngString` for decoding PNG image data into a `Framebuffer`
- Re-export all submodules from the top-level `LuauGl` table so consumers can access them via the package entry point

## 0.0.2
- Remove .luaurc from Wally package to fix transitive dependency resolution
- Bump dependencies and update submodules

## 0.0.1
- Initial release
